<%- include('layouts/header-edit') %>

    <h2 class="mt-3">Daftar Visi Misi</h2>
    <div class="table-responsive">
        <table class="table table-bordered align-middle text-center">
            <thead class="table-dark">
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">Nama</th>
                    <th scope="col">Detail</th>
                    <th scope="col">Ubah</th>
                    <th scope="col">Hapus</th>
                </tr>
            </thead>
            <tbody>
                <% for(let row of rows){ %>
                    <tr>
                        <td>
                            <% let no = rows.indexOf(row) %>
                            <%= no+1 %>
                        </td>
                        <td class="text-start">
                            <%= row.nama_visimisi %>
                        </td>
                        <td class="text-start">
                            <%- row.detail_visimisi %>
                        </td>
                        <td>
                            <a class="nav-link" href="/dashboard/editvisimisi/<%= row.id_visimisi %>">
                                <span data-feather="edit"></span>
                            </a>
                        </td>
                        <td>
                            <a class="nav-link text-danger" href="/dashboard/deletevisimisi/<%= row.id_visimisi %>"
                                onclick="return confirm('Yakin?');">
                                <span data-feather="trash"></span>
                            </a>
                        </td>
                    </tr>
            </tbody>
            <% } %>
        </table>
        <h2 class="pt-5">Tambah Visi Misi</h2>
        <form action="/dashboard/createvisimisi" method="post">
            <div class="mb-3">
                <label for="namaVisiMisi" class="form-label">Nama</label>
                <input type="text" class="form-control" name="nama">
            </div>
            <div class="mb-3">
                <label for="detailVisiMisi" class="form-label">Detail Visi Misi</label>
                <textarea class="form-control" name="detail" rows="10" id="tiny"></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Tambah</button>
        </form>
    </div>


    <%- include('layouts/footer') %>