<%- include('layouts/header') %>

    <h2>Edit Visi Misi</h2>
    <div class="table-responsive">
        <% for(row of rows) { %>
            <form action="<%= row.id_komentar %>" method="post">
                <div class="col-md-4 mb-3">
                    <label for="namaVisiMisi" class="form-label">Nama</label>
                    <input type="text" class="form-control" name="nama" value="<%= row.nama_komentar %>" disabled>
                </div>
                <div class="input-group mb-3">
                    <label class="input-group-text" for="inputGroupSelect01">Aktif Komentar</label>
                    <select class="form-select" name="aktif">
                        <option value="yes">yes</option>
                        <option value="no">no</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary">Update</button>
            </form>
            <% } %>
    </div>

    <%- include('layouts/footer') %>